"""court_ocr — OCR-конвейер для сканов судебных приказов в CSV.

Пакет содержит:
    render   — рендер страниц PDF в изображения (через PyMuPDF, poppler не нужен);
    orient   — определение и исправление поворота (OSD + деузление на 45/90°);
    ocr      — вызов встроенного Tesseract, извлечение текста и уверенности;
    extract  — извлечение полей судебного приказа регулярными выражениями;
    pipeline — обработка страниц в несколько потоков и запись CSV.
"""

__version__ = "1.0.0"
